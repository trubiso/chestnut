import std::io;
import std::fmt::*;

i32 get_a_number() unsafe => 3;

void hold_knife() unsafe {
	let x = 9;
}

void print<T>(T thing) unsafe {
	let x = thing;
	unsafe {
		cpp "std::cout << thing << std::endl;";
	}
}

void doo<T>() {

}

void main() {
	i32 mut r;
	doo<i32>();
	unsafe {
		hold_knife();
		r = get_a_number();
		print<i32>("e");
	}

	// TODO: lex /***/ properly
}