import std::io;
import std::fmt::*;

i32 get_a_number() unsafe => 3;

void hold_knife() unsafe {
	let x = 9;
}

void main() {
	i32 mut r;
	unsafe {
		hold_knife();
		r = get_a_number();
		cpp "std::cout << 1 + 2 + 3 << std::endl;";
	}

	// TODO: lex /***/ properly
}