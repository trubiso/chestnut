i32 double_value(i32 x)  pure => x * 2;
i32 quarter(i32 x) pure => x / 4;

void main() {
	let x = 10;
	let y = 20;
	let z = quarter(double_value(x) + y + x);
	mut a = 10;
	a *= 2;
	a -= 5;
	a /= 3;
	a += 1 + a;
}
