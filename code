import std::io;
import std::fmt::*;

i32 get_a_number() unsafe => 3;

void hold_knife() unsafe {
	let x = 9;
}

void main() {
	let r = get_a_number();
	hold_knife();
	unsafe {
		hold_knife();
	}

	// TODO: lex /***/ properly
}