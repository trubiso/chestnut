def source ::source;

export module my_submodule {
	export func sum_numbers(a: uint32, b: uint32) uint32 => a + b;
	export func sum_numbers(a: uint16, b: uint16) uint16 => a + b;
	func sum_numbers(x: uint64) uint64 => x + x;
}

export func putc(c: char) void;
