// def source ::source;

export module my_submodule {
	func sum_numbers(a: uint64, b: uint64) uint64 => a;
	func sum_numbers(x: uint64) uint64 => sum_numbers(x, x);
}

export func putc(c: char) void {
	const a: uint64 = 2;
	::my_module::my_submodule::sum_numbers(a, 3);
}
